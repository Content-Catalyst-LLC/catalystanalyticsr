# Catalyst Analytics R v0.5.0 R CMD check repair

This repaired v0.5.0 payload resolves defects found by the authoritative macOS `R CMD check` run after the original v0.5.0 package installation.

## Repairs

- Updated `test-export_catalyst_bundle.R` to expect manifest schema `1.2.0`.
- Updated `test-comparison-export.R` to expect package version `0.5.0`.
- Qualified `stats::setNames()` and `stats::reorder()` calls.
- Registered `absolute_delta` as a known plotting binding.
- Corrected `export_stress_test()` to call `export_scenario_comparison()` through its supported arguments and output structure.
- Added a stress-export regression test.
- Added complete argument documentation to the uncertainty and stress-test Rd topics.

The repository and R package version remain `0.5.0`. The WordPress demo remains `1.4.0` because no browser-plugin code changed.
