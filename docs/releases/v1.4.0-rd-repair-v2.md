# Catalyst Analytics R v1.4.0 documentation repair V2

This same-version repair removes the final malformed Rd token from
`man/export_policy_evaluation.Rd`. The prior file ended with the literal
characters `\n` after its final keyword section. The repaired file uses normal
line breaks and complete multiline sections.

The release contract now checks every Rd file for stray escaped-newline tokens,
requires a real terminal newline, and validates balanced usage blocks.
