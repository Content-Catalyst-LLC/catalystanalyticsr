# Catalyst Analytics R v1.5.0 Repair

This same-version repair updates the production-readiness test to match the declared WordPress companion mapping:

- Repository/R package: `1.5.0`
- WordPress companion: `2.5.0`

The compatibility manifest and release manifest were already correct. Only the stale test expectation was changed. A release-contract guard now rejects a return to the old `2.4.0` expectation.
