# Migration from v0.9.0 to v1.0.0

Existing v0.9.0 project, scenario, dataset, and publication contract version 1.0.0 records remain supported. Package metadata advances to 1.0.0. Update tests and integrations that assert the package version. Use `catalyst_api_manifest()` to distinguish stable and experimental exports and `catalyst_compatibility_manifest()` to inspect supported contracts. WordPress companion v2.0.0 maps to repository v1.0.0.
